import React from 'react';

// Testimonials are managed by the admin team and only approved testimonials are ever shown here.
// This starter ships with no testimonials hard-coded, since none should be fabricated.
const Testimonials = () => (
  <div className="page container section">
    <span className="eyebrow">Client feedback</span>
    <h1>Testimonials</h1>
    <p>
      We're just getting started collecting feedback from clients. Approved testimonials will
      appear here as projects are completed.
    </p>
  </div>
);

export default Testimonials;
